

	when (pc == 0) then { op = OP_LDA;	arg = ^b0111;	}
	when (pc == 1) then { op = OP_LDB;	arg = ^b0110;	}
	when (pc == 2) then { op = OP_DECB;			}
	when (pc == 3) then { op = OP_JC	arg = 6;	}
	when (pc == 4) then { op = OP_INCA;			}
	when (pc == 5) then { op = OP_JMP;	arg = 2;	}
	when (pc == 6) then { op = OP_LDB;	arg = 0;	}
	when (pc == 7) then { op = OP_JMP;	arg = 7;	}

